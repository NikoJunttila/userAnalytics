// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.19.1
// source: domain_follows.sql

package database

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createDomainFollow = `-- name: CreateDomainFollow :one

INSERT INTO domain_follows (id, created_at, user_id, domain_id)
VALUES ($1, $2, $3, $4)
RETURNING id, created_at, user_id, domain_id
`

type CreateDomainFollowParams struct {
	ID        uuid.UUID
	CreatedAt time.Time
	UserID    uuid.UUID
	DomainID  uuid.UUID
}

func (q *Queries) CreateDomainFollow(ctx context.Context, arg CreateDomainFollowParams) (DomainFollow, error) {
	row := q.db.QueryRowContext(ctx, createDomainFollow,
		arg.ID,
		arg.CreatedAt,
		arg.UserID,
		arg.DomainID,
	)
	var i DomainFollow
	err := row.Scan(
		&i.ID,
		&i.CreatedAt,
		&i.UserID,
		&i.DomainID,
	)
	return i, err
}

const getDomainsForUser = `-- name: GetDomainsForUser :many
SELECT id, created_at, user_id, domain_id FROM domain_follows WHERE user_id = $1
`

func (q *Queries) GetDomainsForUser(ctx context.Context, userID uuid.UUID) ([]DomainFollow, error) {
	rows, err := q.db.QueryContext(ctx, getDomainsForUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []DomainFollow
	for rows.Next() {
		var i DomainFollow
		if err := rows.Scan(
			&i.ID,
			&i.CreatedAt,
			&i.UserID,
			&i.DomainID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
